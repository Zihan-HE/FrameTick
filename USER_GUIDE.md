# 拾刻 FrameTick v0.5.8 使用说明

## 启动

1. 完整解压收到的文件夹。
2. 双击 `FrameTick.exe`。
3. `_internal` 文件夹必须与 EXE 放在一起，不能删除。

## 使用

1. 选择要拍摄的显示器或应用窗口。
2. 设置录制时长以及秒、分钟或小时单位。
3. 设置截图间隔以及秒、分钟或小时单位。
4. 选择输出模式：保留图片、视频或 GIF。
5. 选择保存位置；视频/GIF 模式还可设置分辨率、格式和文件名前缀。
6. 点击“开始录制”。到达设定时长后会自动停止。

图片模式会把每张 PNG 直接保存到目标文件夹，文件名为截图时间。视频和 GIF 模式会在录制结束后自动生成成片。媒体模式默认勾选“生成后删除临时图片”；取消勾选后，图片会保留在输出文件夹内的 `_frames` 子文件夹。

图片录制结束，或视频/GIF 生成与压缩完成后，Windows 会发送完成通知，并在通知中显示保存文件夹。

## 后台运行

关闭主窗口后，软件会继续留在 Windows 系统托盘。灰色中心表示待机，红色中心表示正在录制，黄色中心表示正在生成视频、GIF 或执行压缩。窗口和托盘标题会同步显示“录制中”或“处理中”。右键托盘图标可以显示窗口、开始/停止录制或退出。

## 常见问题

- Windows 首次运行可能显示未知发布者提示，因为当前版本没有购买代码签名证书。
- 如果找不到新接入的显示器或刚打开的窗口，请点击“刷新”。
- 被拍摄窗口最小化或关闭后，该次窗口录制会停止。
- 请确认目标磁盘有足够空间，尤其是长时间图片模式和高分辨率 GIF。
- 如果需要发送 GIF 到聊天软件，建议选择 1280×720 或更低分辨率，并选择“小文件”压缩档位。
- 本软件不会上传截图；所有文件均保存在本机。

## 系统要求

- 64 位 Windows 10 或 Windows 11。
- 不需要安装 Python。
